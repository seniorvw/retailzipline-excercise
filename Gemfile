source 'https://rubygems.org'

ruby '~> 3.3'  # Adjust as needed for your Ruby version

# We're only using standard library gems, but listing them for clarity
gem 'csv'

group :development, :test do
  gem 'minitest', '~> 5.14'
  gem 'rake', '~> 13.0'
end